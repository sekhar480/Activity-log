select python interpreter 3.9.7
python -m venv my_env
dir
for windows : my_env\Scripts\activate.bat
my_env\Scripts\python.exe -m pip install --upgrade pip
pip install Django==4.1
django-admin startproject Daily_log
cd Daily_log
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
for ip and different settings------   python manage.py runserver 127.0.0.1:8001 \--settings=mysite.settings

for application creation:python manage.py startapp activitylog_app

python manage.py createsuperuser

for filestorage coding.....add in project settings.py filestorage
import os
MEDIA_ROOT =  os.path.join(BASE_DIR, 'media')
MEDIA_URL = '/media/'

and in project urls.py add the following code

from django.conf import settings
from django.conf.urls.static import static
if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL,document_root=settings.MEDIA_ROOT)

'django.middleware.locale.LocaleMiddleware', in settings.py middleware
